import Node

def init_node(name, neighbors, ip, socket):
    # new_node = Node.Node(name, neighbors, ip, socket)
    # print("new_node", new_node)
    return Node.Node(name, neighbors, ip, socket)


# def send_route_table(sender, receiver, state):

